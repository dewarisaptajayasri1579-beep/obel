import 'package:flutter/material.dart';

/// Brand tokens for Obbel Coffee & Milk — Petugas Booth app.
/// Primary hijau + putih, kuning/merah hanya untuk warning/error.
class AppColors {
  AppColors._();

  static const Color primary = Color(0xFF1B6B3F);
  static const Color primaryDark = Color(0xFF0E4027);
  static const Color primaryLight = Color(0xFFE6F2EB);

  static const Color accentOrange = Color(0xFFF2994A);

  static const Color success = Color(0xFF2E7D32);
  static const Color warning = Color(0xFFE0A500);
  static const Color critical = Color(0xFFE53935);
  static const Color info = Color(0xFF2F80ED);

  static const Color background = Color(0xFFF7F8F5);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color border = Color(0xFFE5E7EB);

  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textOnPrimary = Color(0xFFFFFFFF);
}
